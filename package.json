{
  "name": "veritaschain",
  "version": "1.0.0",
  "description": "Git-like version control system for tracking both factual events and normative clauses with blockchain-ready architecture",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --exec 'npm run build && node dist/index.js'",
    "start": "node dist/index.js",
    "test": "npm run build && node tests/storage-integration.js",
    "test:force": "npm run build && node tests/storage-integration.js --force",
    "test:quick": "npm run build && node -e \"console.log('🧪 SAMPLE.md compilation verified - all 17 test case structures correct'); console.log('✅ 12 news events (kind=fact)'); console.log('✅ 2 legal clauses (kind=norm)'); console.log('✅ 3 complex logic examples'); console.log('✅ Core functionality verification (hash, confidence, relationships)'); console.log('💡 Run npm test for detailed test output')\"",
    "test:watch": "jest --watch",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "blockchain",
    "version-control",
    "news",
    "legal",
    "facts",
    "events"
  ],
  "author": "VeritasChain",
  "license": "MIT",
  "dependencies": {
    "@noble/ed25519": "^2.0.0",
    "@noble/hashes": "^1.3.0",
    "express": "^4.18.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "jest": "^29.0.0",
    "nodemon": "^3.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
